---
sidebar_position: 5
---

# ImageNode

Displays an image. Make sure the image is loaded or provide a `src`.

## Example

```ts
const img = engine.nodes.addImage({
  x: 100,
  y: 100,
  width: 180,
  height: 120,
  src: '/logo.png',
});
```
