---
sidebar_position: 3
---

# Grid Plugin

Displays a background grid on a dedicated non-transformed layer.

## Usage

```ts
import { GridPlugin } from '@flowscape-ui/core-sdk';

const engine = new CoreEngine({ container, plugins: [new GridPlugin()] });
```

## Options

Depending on implementation, you can configure spacing, color, and visibility.
