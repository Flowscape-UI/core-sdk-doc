---
sidebar_position: 4
---

# Ruler Plugin

Adds rulers and guides to assist alignment.

## Usage

```ts
import { RulerPlugin } from '@flowscape-ui/core-sdk';

const engine = new CoreEngine({ container, plugins: [new RulerPlugin()] });
```

For performance tips, see `RULER_OPTIMIZATION.md` in the repository.
